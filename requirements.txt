# ── Core (required to run the Streamlit app) ──────────────────────────────────
streamlit>=1.55
openai>=1.40
httpx>=0.27
pandas>=2.0
plotly>=5.20
PyMuPDF>=1.24          # imported as "fitz" — PDF rendering + report generation
python-dotenv>=1.0     # loads secrets from a local .env file

# ── Optional (only for the REST API channel) ──────────────────────────────────
# fastapi>=0.110            # api_server.py     (REST API / ERPNext sidebar)
# uvicorn>=0.29             # api_server.py
