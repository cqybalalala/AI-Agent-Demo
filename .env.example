# Copy this file to ".env" and fill in your own keys.
# config.py loads these via python-dotenv; .env is git-ignored and never committed.

# OpenAI-compatible LLM keys (the app uses OpenRouter by default)
OPENROUTER_API_KEY=
CHUTES_API_KEY=
